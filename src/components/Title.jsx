import "./Title.css";

const Title = () => {
    return (
        <div className="title-bar">
            <h1 className="title-text">Repo Line Counter</h1>
        </div>
    );
};

export default Title;
